<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%=  meta[:title] %></title>
    <link href="/assets/styles.css" rel="stylesheet">
  </head>
  <body class="page page--home">
    <div class="main-content">
      <header class="header">
        <div class="header__content">
          <h1 class="header__title"><%= meta[:title] %></h1>

          <%= content %>

          <div class="header__social">
            <a href="<%= meta[:github_url] %>" target="_blank">
              GitHub
            </a>
          </div>
        </div>
        <img class="header__image" src="<%= meta[:image_url] %>" alt="Juanra">
      </header>

      <main class="posts-list">
        <% site.posts.each do |post| %>
          <a href="<%= post[:url] %>" class="post-item">
            <h2 class="post-item__title"><%= post[:title] %></h2>
            <h3 class="post-item__summary"><%= post[:desc] %></h3>
            <div class="post-item__date"><%= post[:date].strftime("%B %d, %Y") %></div>
          </a>
        <% end %>
      </main>
    </div>
  </body>
</html>
